[package]
name = "ostrya"
description = "Pure-Rust async reimplementation of the ostree repository library"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[features]
default = ["smol"]
smol = ["ostrya-rt/smol"]
tokio = ["ostrya-rt/tokio"]

[dependencies]
ostrya-core = { path = "../ostrya-core" }
ostrya-composefs = { path = "../ostrya-composefs" }
ostrya-rt = { path = "../ostrya-rt", default-features = false }
thiserror = "2"
rustix = { version = "1", default-features = false, features = ["std", "fs"] }
sha2 = { version = "0.10", default-features = false, features = ["std"] }
async-compression = { version = "0.4", default-features = false, features = [
    "deflate",
    "futures-io",
] }
pin-project-lite = "0.2"
futures-io = "0.3"

[dev-dependencies]
ostrya-rt = { path = "../ostrya-rt", default-features = false }
futures-lite = "2"
