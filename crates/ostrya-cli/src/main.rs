#![forbid(unsafe_code)]

//! The `ostree`-compatible command-line front-end.
//!
//! This binary is a late-phase deliverable (see `docs/port-plan.md`, phase 16).
//! No subcommands are implemented yet.

fn main() {
    eprintln!("ostrya-cli: not implemented yet (see docs/port-plan.md, phase 16)");
    std::process::exit(1);
}
